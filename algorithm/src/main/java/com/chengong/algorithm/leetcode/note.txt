1. binary search
    a. index i, j: mid = i + (j-i)/2; mid = i + (j-i+1)2;
2. contiguous -> DP: sub-array closed at i
3. math: overflow(+,* use -,/ instead), divide by zero
4. graph, matrix, linked list(graph): prevent circle
5. Java pass by copy of refer: using class variable
6. Tree: stop at null or leaf node.
7. LinkedList: two node same will usually break logic